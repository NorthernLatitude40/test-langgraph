name: wechat_pay_flow
target_agent_endpoint: "http://localhost:8000/agent" # 你的 LangGraph Agent 運行端口
timeout: 15 # 標準 YAML 註解請使用井字號，且前方不要有逗號

test_cases: # 👈 這裡改成對齊腳本的 test_cases
  - input: "張三買了什麽。"
    expected: "觸發get_customer_products，返回張三購買物品。"
